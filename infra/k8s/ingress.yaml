apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: authen-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: authen.yourdomain.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: authen-service
                port:
                  number: 80
